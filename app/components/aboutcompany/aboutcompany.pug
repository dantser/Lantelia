include ../../blocks/section-head/section-head
include ../../blocks/news-card/news-card

mixin aboutcompany()
  +b.SECTION.aboutcompany&attributes(attributes)
    .container
      +section-head('Компания Lantelia', 'Информация')
      +grid
        +col(6)
          +news-card-large(getData('news-card-large'))
        +col(6)._nested
          +grid
            +col(6)
              +news-card(getData('news-card'))._colored._blue
            +col(6)
              +news-card(getData('news-card'))._colored._blue
          +grid
            +col(6)
              +news-card(getData('news-card'))._colored._pink
            +col(6)
              +news-card(getData('news-card'))._colored._gray
          //- +news-card(getData('news-card'))._colored._green
